<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.project.module</groupId>
    <artifactId>modules</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>modules</name>
    <packaging>pom</packaging>
    <description>modules</description>

    <properties>
        <jdk.version>1.8</jdk.version>
        <lombok.version>1.18.4</lombok.version>
        <spring.version>5.1.5.RELEASE</spring.version>
        <spring.boot.version>2.2.1.RELEASE</spring.boot.version>
        <spring-boot-admin.version>2.2.0</spring-boot-admin.version>
        <mybatis-spring-boot-starter.version>2.2.0</mybatis-spring-boot-starter.version>
        <mysql-connector-java.version>8.0.17</mysql-connector-java.version>
        <mybatis-starter.version>2.1.1</mybatis-starter.version>
        <spring-restdocs-mockmvc.version>2.0.4.RELEASE</spring-restdocs-mockmvc.version>
        <argLine>-Dfile.encoding=UTF-8</argLine>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <druid-spring-boot-starter.version>1.1.21</druid-spring-boot-starter.version>
        <swagger.version>1.5.24</swagger.version>
        <springfox-swagger.version>2.9.2</springfox-swagger.version>
        <pagehelper.version>1.2.12</pagehelper.version>
        <dubbo-start.version>2.7.4.1</dubbo-start.version>
        <dubbo.version>2.7.4.1</dubbo.version>
        <sentinel.version>1.7.0</sentinel.version>
        <sentinel-cloud.version>2.1.1.RELEASE</sentinel-cloud.version>
        <seata.version>0.8.0</seata.version>
        <kryo-serializers.version>0.43</kryo-serializers.version>
        <curator.version>4.2.0</curator.version>
        <logstash-logback-encoder.version>6.2</logstash-logback-encoder.version>
        <swagger-dubbo.version>2.0.1</swagger-dubbo.version>
        <logback-classic.version>1.2.3</logback-classic.version>
        <logback-core.version>1.2.3</logback-core.version>
    </properties>

    <build>
        <!-- <finalName>${project.build.finalName}</finalName> -->
        <sourceDirectory>src/main/java</sourceDirectory>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <testSourceDirectory>src/test/java</testSourceDirectory>
        <testResources>
            <testResource>
                <directory>src/test/resources</directory>
            </testResource>
        </testResources>
        <outputDirectory>target/classes</outputDirectory>
        <testOutputDirectory>target/test-classes</testOutputDirectory>

        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${jdk.version}</source>
                    <target>${jdk.version}</target>
                    <encoding>utf-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-source-plugin</artifactId>
                <configuration>
                    <attach>true</attach>
                </configuration>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <configuration>
                    <encoding>utf-8</encoding>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <mainClass>com.ynairways.platform.ApplicationStarter</mainClass>
                    </configuration>
                    <executions>
                        <execution>
                            <id>repackage</id>
                            <configuration>
                                <classifier>exec</classifier>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <modules>
        <module>druid</module>
        <module>runner</module>
        <module>monitor</module>
        <module>redis</module>
        <module>message</module>
        <module>mockmvc</module>
        <module>elasticsearch</module>
        <module>mysql</module>
        <module>swagger</module>
        <module>demo</module>
    </modules>


</project>
